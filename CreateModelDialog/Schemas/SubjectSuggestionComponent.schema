{
  "$schema": "https://schemas.botframework.com/schemas/component/v1.0/component.schema",
  "$role": "implements(Microsoft.IDialog)",
  "title": "SubjectSuggestionComponent",
  "description": "This will return a list of suggested actions with all the subjects currently existing in the model. subjects can be filtered using constructors",
  "type": "object",
  "additionalProperties": false,
  "properties": {    
  }
}