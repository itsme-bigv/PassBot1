{
  "$schema": "https://schemas.botframework.com/schemas/component/v1.0/component.schema",
  "$role": "implements(Microsoft.IDialog)",
  "title": "SubjectSuggestions",
  "description": "This will send a list of suggested actions with all the subjects currently existing in the model",
  "type": "object",
  "additionalProperties": false,
  "properties": {    
  }
}